%h1 Editing timetabled_session

= render 'form'

= link_to 'Show', @timetabled_session
\|
- if current_user.lecturer?
    = link_to 'Back', lecturer_path
- if current_user.admin?
    = link_to 'Back', admin_path
