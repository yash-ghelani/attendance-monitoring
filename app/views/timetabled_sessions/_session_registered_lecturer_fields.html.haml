.nested-fields
  = f.association :user, collection: User.where(lecturer: true), label: false, placeholder: "Enter Lecturer email"
  = link_to_remove_association "remove lecturer", f, class: 'btn btn-danger mb-3'