development:
  adapter: postgresql
  database: team_11_dev
  min_messages: warning
  
  # comment/remove these lines if running WSL on Windows
  host: localhost
  username:

test:
  adapter: postgresql
  database: team_11_test
  min_messages: warning

  # comment/remove these lines if running WSL on Windows
  host: localhost
  username: